import { CanActivate, Router } from "@angular/router";
import { AuthConfig } from "../auth.config";
import { TokenService } from "../_services/token.service";
export declare class LoggedOutAuth implements CanActivate {
    private _config;
    private _tokenService;
    private _router;
    constructor(_config: AuthConfig, _tokenService: TokenService, _router: Router);
    canActivate(): boolean;
}
export declare class LoggedInAuth implements CanActivate {
    private _config;
    private _tokenService;
    private _router;
    constructor(_config: AuthConfig, _tokenService: TokenService, _router: Router);
    canActivate(): boolean;
}
